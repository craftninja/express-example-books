h1  My Books

ul
  each book in books
    if book.haveRead
      li
        a(href="books/#{book.id}")= book.title
        =" (" + book.pages + "pages) - read"
    else
      li
        a(href="books/#{book.id}")= book.title
        =" (" + book.pages + " pages) - unread"
